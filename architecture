build
      -> build.sh (linux compile)
      -> build.bat (windows compile)
            -> main.cpp (main file)
                  -> Controller (Where the core running logic is (Login, Registration, Call Menu))
                        -> MainMenu (Validation and redirects the user to the desired function or calls classes & functions.)